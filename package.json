{
  "name": "finance-gpt-agent",
  "version": "1.0.0",
  "description": "AI-powered Financial Research Platform",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "dev": "nodemon server/server.js",
    "client": "cd client && live-server --port=8080",
    "deploy": "./deploy.sh",
    "health": "node -e \"const axios=require('axios');const check=async()=>{try{await axios.get('http://localhost:3001/api/news?ticker=AAPL');console.log('✅ Health check passed');}catch(e){console.log('❌ Health check failed:',e.message)}};check();\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "pinecone-client": "^1.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "live-server": "^1.2.2"
  }
}
